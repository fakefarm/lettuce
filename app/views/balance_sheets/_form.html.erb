<%= form_for(@balance_sheet) do |f| %>
  <% if @balance_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@balance_sheet.errors.count, "error") %> prohibited this balance_sheet from being saved:</h2>

      <ul>
      <% @balance_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :timeline %><br>
    <%= f.text_field :timeline %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
