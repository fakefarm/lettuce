<%= form_for(@transfer) do |f| %>
  <% if @transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
      <% @transfer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :equity_id %><br>
    <%= collection_select( :equity, :equity_id, Equity.all, :id, :nickname, {} ) -%>
  </div>
  <div class="field">
    <%= f.label :liability_id %><br>
    <%= collection_select( :equity, :liability_id, Liability.all, :id, :nickname, {} ) -%>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
