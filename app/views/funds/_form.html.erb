<%= form_for(@fund) do |f| %>
  <% if @fund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fund.errors.count, "error") %> prohibited this fund from being saved:</h2>

      <ul>
      <% @fund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= collection_select( :fund, :account_id, Equity.all, :id, :nickname, {} ) -%>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :deposit_date %><br>
    <%= f.date_select :deposit_date %>
  </div>
  <div class="field">
    <%= f.label :vendor_id %><br>
    <%= collection_select( :fund, :vendor_id, Vendor.all, :id, :name, {} ) -%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
